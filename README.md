# Terminal GenAI Chatbot

A simple streaming chatbot built with Python and OpenAI's gpt-4.1-nano model.

## Features
- Uses streaming responses for real-time typing effect
- Runs in your terminal
- Environment-based API keys

## Setup

```bash
python -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
